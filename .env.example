# ═══ Discord Application (https://discord.com/developers/applications) ═══
CLIENT_ID=
CLIENT_SECRET=
BOT_TOKEN=

# DEBE ser IDÉNTICO al Redirect de OAuth2 en el Portal (copiar/pegar)
# Local:
REDIRECT_URI=http://localhost:3000/auth/callback
# Producción (Render etc):
# REDIRECT_URI=https://tu-app.onrender.com/auth/callback

PORT=3000
SESSION_SECRET=pon_un_texto_largo_y_aleatorio_aqui

# Si estás en HTTPS (Render/Railway) y la cookie no se guarda:
# COOKIE_SECURE=true
# NODE_ENV=production
